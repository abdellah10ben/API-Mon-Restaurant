spring:
  datasource:
    url: jdbc:mysql://db:3306/mon_restaurant?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: bouyanzer
    password: bouyanzer
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: true

  main:
    web-application-type: servlet

server:
  port: 8080
  error:
    include-message: always
    
management:
  endpoints:
    web:
      exposure:
        include: mappings,health,info


